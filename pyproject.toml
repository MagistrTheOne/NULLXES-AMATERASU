[project]
name = "amaterasu"
version = "0.1.0"
description = "NULLXES AMATERASU-32B v0.1 Embodied Agency Foundation Model"
readme = "AMATERASU_EAFM_ARCHITECTURE_v0.1.md"
requires-python = ">=3.10"
authors = [{ name = "MagistrTheOne | NULLXES" }]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["amaterasu*"]
